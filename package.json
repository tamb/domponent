{
  "name": "domponent",
  "version": "1.0.0-alpha.7",
  "description": "Build components with the HTML you already have.",
  "homepage": "https://github.com/tamb/domponent",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-all": "node clean.js && rollup -c rollup.config.js && npm pack && cd example && npm install --force && webpack && firefox index.html"
  },
  "files": [
    "dist"
  ],
  "author": "tamb",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "rimraf": "^2.6.3",
    "rollup": "^1.18.0",
    "rollup-plugin-babel": "^4.3.3",
    "rollup-plugin-terser": "^5.1.1"
  }
}
