{
  "name": "domponent",
  "version": "1.0.0-alpha.16",
  "description": "Build components with the HTML you already have.",
  "keywords": [
    "component",
    "ui",
    "react",
    "reactive",
    "lifecyle",
    "server-side",
    "html",
    "data"
  ],
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/tamb/domponent.git"
  },
  "homepage": "https://github.com/tamb/domponent",
  "main": "dist/domponent.es.js",
  "umd:main": "dist/domponent.min.js",
  "unpkg": "dist/domponent.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c rollup.config.js",
    "build:html": "node clean.js && rollup -c rollup.config.js && npm pack && cd example/html && npm install --force && webpack && firefox index.html"
  },
  "files": [
    "dist"
  ],
  "author": "tamb",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "rimraf": "^2.6.3",
    "rollup": "^1.18.0",
    "rollup-plugin-babel": "^4.3.3",
    "rollup-plugin-terser": "^5.1.1"
  }
}
